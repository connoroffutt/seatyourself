


<% if current_user %>
  <%= form_for([@restaurant, @reservation]) do |f|%>

    <button type="button" onclick="reservation#create">Reservation</button>
<% else %>
    <p>Please <%= link_to "log in", new_session_path %> to make a reservation. </p>
<% end %>

<% end %>
