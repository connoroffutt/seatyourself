

<%= form_for([@restaurant, @reservations]) do |f| %>
  <div class="field">
    <%= f.label :datetime %><br />
    <%= f.datetime_field :datetime %>
  </div>
  <div class="action">
    <%= f.submit "Reserve" %>
  </div>


<% end %>
